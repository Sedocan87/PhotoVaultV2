import React from 'react';

const RestoreBackupModal: React.FC = () => {
    return (
        <div>
            {/* Add your restore backup modal content here */}
        </div>
    );
};

export default RestoreBackupModal;
